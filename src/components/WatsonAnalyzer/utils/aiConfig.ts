/**
 * The single AI model used for both text optimization and domain-entity
 * extraction. Hardcoded on purpose — no provider/model picker to get wrong.
 * Change these two lines to switch model (e.g. "claude-sonnet-4-6" for a
 * cheaper/faster option).
 */
export const AI_MODEL = "claude-opus-4-7";
export const AI_MODEL_LABEL = "Claude Opus 4.7";
